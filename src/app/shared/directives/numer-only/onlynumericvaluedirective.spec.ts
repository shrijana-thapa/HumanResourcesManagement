import { Onlynumericvaluedirective } from './onlynumericvaluedirective';

describe('Onlynumericvaluedirective', () => {
  it('should create an instance', () => {
    const directive = new Onlynumericvaluedirective();
    expect(directive).toBeTruthy();
  });
});
